# ТРЯСЦЯ v1.1.0

`v1.1.0` — стабільний hardening-реліз без breaking-змін основного skill/installer contract.

## Основне

- 100 records: 95 `accepted`, 5 `candidate`, 95 runtime;
- атомарні checksum-verified installers для Codex, Claude Code, Hermes Agent та OpenClaw;
- executable behavioral regression gate для public/security/irreversible/no-user-insult/intensity і `lite/full/ultra/normal`;
- формальний compatibility contract для 4 stable агентів;
- постійний tag-driven release pipeline з pinned GitHub Actions і SHA256 release assets;
- protected `main`: PR-only, required `corpus`, force-push/delete blocked, no bypass.

## Сумісність

Stable:

- Codex;
- Claude Code;
- Hermes Agent;
- OpenClaw.

Breaking changes відносно `v1.0.0` відсутні. Деталі — у `CHANGELOG.md`, `docs/COMPATIBILITY.md` та `docs/RELEASING.md`.
